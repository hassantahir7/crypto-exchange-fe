// src/theme.ts
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: "Outfit, sans-serif", // Set the global font family
  },
  // Other theme customizations...
});

export default theme;
